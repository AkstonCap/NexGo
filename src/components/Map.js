import { useEffect } from 'react';

export default function Map() {
  useEffect(() => {
    // Initialize the map using the external API
  }, []);

  return <div id="map" style={{ height: '500px' }} />;
}